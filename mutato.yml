---
containers:
  - docker:
      name: mutato
      file: Dockerfile
      uri: stelligent/mutato
      events:
        post-build: npm-deploy
---
actions:
  - docker:
      name: npm-deploy
      container: node:lts
      cmd: npm install
